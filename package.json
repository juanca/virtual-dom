{
  "name": "virtual-dom",
  "version": "0.0.0",
  "description": "My implementation of a virtual DOM. Only updates the actual DOM when necessary.",
  "main": "index.js",
  "scripts": {
    "start": "webpack-dev-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan Carlos Medina",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^3.8.1",
    "webpack-dev-server": "^2.9.4"
  }
}
